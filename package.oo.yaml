version: 0.0.2
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  {}
name: social-media-publisher
description: Social Media Publisher
displayName: Social Media Publisher
repository: https://github.com/oomol-blocks/sync-post.git
releaseNotes: upload icon
icon: ./icon.png
